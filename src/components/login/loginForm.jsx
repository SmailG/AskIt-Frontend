import './style.css';
import React, { Component }  from 'react';
// import { Button, Icon, Menu, Segment, Sidebar, Checkbox, Divider, Input } from 'semantic-ui-react'

const authForm = ({  }) => {


    return (
        <div>
        </div>

    );
}

export default authForm;